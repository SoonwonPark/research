This is a python code for generating Control Flow Graph.
The reuslt is a dot file of a Windows PE.
You can render a graph file(.jpg or .pdf) using dot command.
ex) dot -Tpdf example.dot -o example.pdf

The example file in the folder is server.exe(Bozok 1.5.1)

